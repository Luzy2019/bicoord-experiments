[build-system]
requires = ["flit_core >=3.7,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "flow_matching_policy"
version = "0.1.0"
description = "Flow matching policy for RoboTwin"
requires-python = ">=3.8"
dependencies = [
    "hydra-core==1.2.0",
    "numba"
]